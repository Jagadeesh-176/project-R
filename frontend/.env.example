# URL of the backend API (see ../backend).
# For testing on your phone, change this to your PC's LAN IP instead of
# localhost, e.g. http://192.168.1.23:8000 — "localhost" on a phone means
# the phone itself, not your PC. See README for the full explanation.
VITE_API_BASE=http://localhost:8000
